# 完整依赖清单（deer-flow 原有 + 平台改造新增）
# 安装：.venv/bin/pip install -r requirements-platform.txt
# 注意：本机 tuna 镜像不通、官方 PyPI 可用，如慢可换 -i https://mirrors.aliyun.com/pypi/simple/

# ---- langchain / langgraph 栈：严格按 uv.lock 锁定，勿升级（1.x 不兼容本项目）----
langchain==0.3.20
langchain-community==0.3.19
langchain-core==0.3.43
langchain-experimental==0.3.4
langchain-mcp-adapters==0.0.9
langchain-openai==0.3.8
langchain-text-splitters==0.3.6
langgraph==0.4.3
langgraph-checkpoint==2.0.25
langgraph-prebuilt==0.1.8
langgraph-sdk==0.1.69
langsmith==0.3.13
openai==1.66.3

# ---- deer-flow 原有依赖 ----
httpx>=0.28.1
readabilipy>=0.3.0
python-dotenv>=1.0.1
socksio>=1.0.0
markdownify>=1.1.0
fastapi>=0.110.0
uvicorn>=0.27.1
sse-starlette>=1.6.5
pandas>=2.2.3
numpy>=2.2.3
json-repair>=0.7.0
jinja2>=3.1.3
duckduckgo-search>=8.0.0
inquirerpy>=0.3.4
arxiv>=2.2.0
mcp>=1.6.0

# ---- 平台改造新增 ----
sqlalchemy>=2.0
greenlet>=3.0
aiosqlite==0.21.0  # 0.22+ 移除 Thread 基类，与 langgraph-checkpoint-sqlite 不兼容
asyncpg>=0.29.0
pyjwt>=2.8.0
langgraph-checkpoint-sqlite>=2.0.0
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary]>=3.1

# ---- 测试 ----
pytest>=7.4.0
pytest-asyncio>=0.23
python-docx>=1.1.0

# ---- 附件解析 / 表格技能 ----
openpyxl==3.1.5
pypdf==5.1.0
python-multipart>=0.0.9  # FastAPI 解析 multipart 上传（/api/attachments）必需

# ---- PPT 直出 / 文档图表 ----
python-pptx>=1.0.2  # /api/ppt 改为 python-pptx 直出，摆脱 marp+chrome
matplotlib>=3.9  # 办公文档嵌入数据图表（Agg 后端出 PNG）
